# 📔 開発・運用覚書（将来の拡張プラン）

本ドキュメントは、現在のシステムの不具合修正履歴、および将来的な機能拡張やレポート出力の統合案を記録するエンジニア・運用担当者向けの覚書です。

---

## 🛠 直近の修正・改善記録

### 1. 分析レポート表示の不具合修正（2026/03/04）
- **事象**: 「分析レポート」タブをクリックしても反応しない、または内容が表示されない。
- **原因**: 
  - `renderMeasureEffectSummary` 関数内での変数参照ミス（`latestMeasure` が未定義）。
  - ブラウザの日付解釈（`/` と `-`）の差異によるデータフィルタリングの失敗。
- **対策**: 
  - 変数名を `targetMeasure` に統一し、ReferenceErrorを解消。
  - 日付文字列をパースする際、`.replace(/\//g, '-')` を挟むことでブラウザ依存を排除。
  - 比較対象（前期間）がない場合でも必ず「現状分析（Fact）」を表示するフォールバックロジックを実装。

### 2. JSONエクスポート機能の拡張（2026/03/04）
- **事象**: 書き出したJSONに「AI分析」や「統計比較」のデータが含まれておらず、外部連携に不十分。
- **改善**: 
  - `analysis` 項目を追加し、AI生成テキスト（Fact/Interpretation/Action）を収録。
  - `measureEffect` 項目を追加し、施策前後の平均値（CVR、CPA等）と変化率を構造化データとして保存。
  - HTMLタグを除去（stripHtml）し、純粋なテキストとして出力。

---

## 🚀 将来の拡張・統合アイデア

### 1. 高品質PDFレポート生成システムとの統合
提供された「A4横スライド形式」のHTMLレポートシステムを統合する案。

- **統合の方向性**:
  - ダッシュボードから「高品質レポート作成」ボタンで、現在のJSONデータを引数として別タブへ渡す。
  - `sessionStorage` を介してダッシュボードの分析結果をレポート側へ即座に反映。
- **改善提案**:
  - **エグゼクティブ・サマリーの追加**: 1ページ目を見るだけで結論（Action）がわかるページ構成。
  - **注釈（アノテーション）機能**: グラフの特定日に吹き出しでコメントを追加できる機能。
  - **テーマカラーカスタマイズ**: クライアントのブランドカラーへ一括変更。

### 2. データ連携の自動化・効率化（検討中）
- **ATOM連携の統合**:
  - ATOMからスプレッドシートへ出力される標準形式（日次データ）を自動パースするインポーターの作成。
  - 「すべての媒体」シートからの媒体横断データの取り込み。
- **20案件規模のスケール設計**:
  - **マスター名簿による一元管理**: 1つのフロントエンドに対し、20個の個別データシートを紐付ける「管制塔」方式。
  - **名寄せ（ID管理）の徹底**: 顧客名ではなく不変の「クライアントID」をキーにしたデータ紐付け。
  - **権限管理**: Google Workspaceドメインによる一括権限付与。
- **Microsoft Clarityとの同期（定性データ統合）**:
  - Clarity Data Export APIを介したスプレッドシートへのデータ自動書き込み。
  - デッドクリック率、怒りのクリック率、スクロール深度などの「ユーザー行動指標」の可視化。
  - GA4の「数値」とClarityの「行動」を突合させた、より精度の高いAI分析。

### 3. コスト最適化と内製化の検討（戦略的メモ）
- **ATOM利用料と内製化のトレードオフ**:
  - 自社API連携（Google Ads API等）によりツール利用料を削減可能。
  - ただし、APIの仕様変更への追従や認証（トークン）維持のメンテナンス工数が発生する。
- **ハイブリッド移行案**:
  - まずは定型化されたATOM経由で20案件を迅速に立ち上げる。
  - 運用安定後、順次コストの高い媒体からスプレッドシート用アドオン（Adveronix等）や直接API連携へ切り替え、利益率を高める。

---

## 📌 運用上の留意点
- **フォントロードの待機**: PDF生成時は `document.fonts.ready` を待ってからキャプチャを行うこと。
- **キャッシュクリア**: 修正を反映させるため、運用担当者には適宜 `Ctrl + F5` による強制リフレッシュを推奨すること。

---
© 2026 マーケティング・ダッシュボード開発チーム
